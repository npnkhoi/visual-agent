Locate every {target_noun} visible in this image.
For each instance, provide the pixel (x, y) coordinates of its center point.
Reply with ONLY a JSON array of [x, y] pairs — no explanation, no markdown.
Example: [[120, 340], [250, 180], [410, 95]]
If there are none, reply with [].
